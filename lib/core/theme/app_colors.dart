import 'package:flutter/material.dart';

abstract class AppColors {
  static const primaryColor = Color.fromRGBO(236, 137, 6, 1);
  static const secondaryColor = Color.fromARGB(255, 10, 10, 10);
  static const hintOrDisableColor = Colors.grey;
  static const hidingColor = Colors.transparent;
  static const txtInsideBtnColor=Colors.white;
}
